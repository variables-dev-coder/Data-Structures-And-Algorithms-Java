package Arrays.DSA_Insertion_Sort;

public class Example {

                    /*

                    public ListNode insertionSortList(ListNode head) {
                    if (head == null || head.next == null) return head;

                    ListNode dummy = new ListNode(0); // Dummy node to build the sorted list
                    ListNode curr = head; // Current node to be inserted

                    while (curr != null) {
                        ListNode prev = dummy;
                        ListNode next = curr.next; // Save next node

                        // Find the correct position to insert `curr`
                        while (prev.next != null && prev.next.val < curr.val) {
                            prev = prev.next;
                        }

                        // Insert `curr` after `prev`
                        curr.next = prev.next;
                        prev.next = curr;

                        curr = next; // Move to the next unsorted node
                    }

                    return dummy.next;
                }

                     */

}
